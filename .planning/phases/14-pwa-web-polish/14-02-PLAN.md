---
phase: 14-pwa-web-polish
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - web/src/App.tsx
  - web/src/components/TabBar.tsx
  - web/src/components/Sidebar.tsx
  - web/src/components/OfflineBanner.tsx
  - web/src/components/UpdateToast.tsx
  - web/src/components/InstallBanner.tsx
  - web/src/components/AppHead.tsx
  - web/src/pages/HomePage.tsx
  - web/src/pages/SkillTreePage.tsx
  - web/src/pages/ProfilePage.tsx
  - web/src/pages/LessonPage.tsx
  - web/src/pages/OnboardingPage.tsx
autonomous: true
requirements:
  - PWA-02
  - PWA-03
  - PWA-04
must_haves:
  truths:
    - "Desktop viewport (1024px+) shows left sidebar navigation instead of bottom TabBar"
    - "Mobile/tablet viewport shows bottom TabBar, no sidebar"
    - "Offline banner appears below GameHeader when network is lost"
    - "Update toast appears when a new service worker is waiting"
    - "Install banner appears on iOS (nudge) and Android (native prompt)"
    - "Each route has a unique page title via react-helmet-async"
    - "All layout uses CSS logical properties for RTL support"
  artifacts:
    - path: "web/src/components/Sidebar.tsx"
      provides: "Desktop sidebar nav at lg+ with NavLink items matching TabBar"
      exports: ["Sidebar"]
      contains: "hidden lg:flex"
    - path: "web/src/components/OfflineBanner.tsx"
      provides: "Amber offline status banner"
      exports: ["OfflineBanner"]
      contains: "role=\"status\""
    - path: "web/src/components/UpdateToast.tsx"
      provides: "PWA update prompt toast using useRegisterSW"
      exports: ["UpdateToast"]
      contains: "useRegisterSW"
    - path: "web/src/components/InstallBanner.tsx"
      provides: "iOS nudge + Android beforeinstallprompt banner"
      exports: ["InstallBanner"]
      contains: "beforeinstallprompt"
    - path: "web/src/components/AppHead.tsx"
      provides: "react-helmet-async wrapper for per-route title/description"
      exports: ["AppHead"]
      contains: "Helmet"
    - path: "web/src/App.tsx"
      provides: "Responsive layout shell with HelmetProvider, Sidebar, banner wiring"
      contains: "HelmetProvider"
  key_links:
    - from: "web/src/components/UpdateToast.tsx"
      to: "virtual:pwa-register/react"
      via: "useRegisterSW hook import"
      pattern: "import.*useRegisterSW.*virtual:pwa-register"
    - from: "web/src/App.tsx"
      to: "web/src/components/Sidebar.tsx"
      via: "Sidebar component in RootLayout"
      pattern: "<Sidebar"
    - from: "web/src/App.tsx"
      to: "react-helmet-async"
      via: "HelmetProvider wrapping router"
      pattern: "HelmetProvider"
    - from: "web/src/components/TabBar.tsx"
      to: "responsive visibility"
      via: "lg:hidden class"
      pattern: "lg:hidden"
---

<objective>
Build all PWA UI components (Sidebar, OfflineBanner, UpdateToast, InstallBanner, AppHead) and wire them into the responsive layout shell. Convert TabBar to hide on desktop, add desktop Sidebar, wrap app in HelmetProvider, and add per-route titles to all pages.

Purpose: Complete the user-facing PWA experience — responsive desktop layout, offline awareness, install prompts, update notifications, and SEO-ready page titles.
Output: Fully responsive app with PWA banners, desktop sidebar, and per-route SEO metadata.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-pwa-web-polish/14-CONTEXT.md
@.planning/phases/14-pwa-web-polish/14-RESEARCH.md
@.planning/phases/14-pwa-web-polish/14-UI-SPEC.md
@.planning/phases/14-pwa-web-polish/14-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From web/src/App.tsx (RootLayout to be modified):
```typescript
import { createBrowserRouter, RouterProvider, Outlet, Navigate } from 'react-router'
import { GameHeader } from './components/GameHeader'
import { LevelUpModal } from './components/LevelUpModal'
import { TabBar } from './components/TabBar'
import { useProgressStore, useHasHydrated } from './store/useProgressStore'

function RootLayout() {
  const pendingLevelUp = useProgressStore(s => s.pendingLevelUp)
  const clearPendingLevelUp = useProgressStore(s => s.clearPendingLevelUp)
  const hasOnboarded = useProgressStore(s => s.hasOnboarded)
  const hasHydrated = useHasHydrated()
  // Returns: <div flex-col min-h-dvh> with GameHeader, main (pb-14), TabBar, LevelUpModal
}
```

From web/src/components/TabBar.tsx (nav items to mirror in Sidebar):
```typescript
const tabs = [
  { to: '/', labelKey: 'tabs.home', icon: '🏠', end: true },
  { to: '/tree', labelKey: 'tabs.skillTree', icon: '🌳', end: false },
  { to: '/profile', labelKey: 'tabs.profile', icon: '👤', end: false },
] as const
```

Router routes (pages that need AppHead):
```typescript
{ path: '/', element: <HomePage /> }
{ path: '/tree', element: <SkillTreePage /> }
{ path: '/profile', element: <ProfilePage /> }
{ path: '/lesson/:id', element: <LessonPage /> }
{ path: '/onboarding', element: <OnboardingPage /> }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PWA UI components — Sidebar, OfflineBanner, UpdateToast, InstallBanner, AppHead</name>
  <files>web/src/components/Sidebar.tsx, web/src/components/OfflineBanner.tsx, web/src/components/UpdateToast.tsx, web/src/components/InstallBanner.tsx, web/src/components/AppHead.tsx</files>
  <read_first>
    - web/src/components/TabBar.tsx (mirror nav items for Sidebar, understand existing pattern)
    - web/src/components/GameHeader.tsx (understand header height for banner positioning)
    - .planning/phases/14-pwa-web-polish/14-RESEARCH.md (Patterns 2-6 with exact code)
    - .planning/phases/14-pwa-web-polish/14-UI-SPEC.md (PWA banner components contract, accessibility contract, copywriting contract)
  </read_first>
  <action>
Create 5 new component files:

**1. `web/src/components/Sidebar.tsx`** — Desktop navigation at lg+:
- Import `NavLink` from `'react-router'` and `useTranslation` from `'react-i18next'`
- Define `navItems` array matching TabBar's `tabs` exactly: `[{ to: '/', labelKey: 'tabs.home', icon: '🏠', end: true }, { to: '/tree', labelKey: 'tabs.skillTree', icon: '🌳', end: false }, { to: '/profile', labelKey: 'tabs.profile', icon: '👤', end: false }]`
- Render `<aside>` with classes: `hidden lg:flex fixed inset-y-0 start-0 w-64 flex-col bg-gray-50 border-e border-gray-200 z-40`
- Top section: `<div className="p-4 border-b border-gray-200">` with `<span className="text-xl font-bold text-indigo-600">PromptPlay</span>`
- Nav section: `<nav aria-label="Main navigation" className="flex-1 py-4">` with NavLink items
- Each NavLink: `className` callback using `isActive` — active: `border-s-2 border-indigo-600 text-indigo-600 font-bold bg-indigo-50`, inactive: `border-s-2 border-transparent opacity-60 hover:opacity-80`
- NavLink layout: `flex items-center gap-3 px-4 py-3 text-sm`
- Icon span: `<span aria-hidden="true">{item.icon}</span>` then `{t(item.labelKey)}`
- Use `end` prop on Home NavLink to prevent false active match (same as TabBar pattern)
- MUST use CSS logical properties: `start-0`, `border-e`, `border-s-2`, `ps-4`, `pe-4` (or `px-4` which maps to logical in Tailwind v4)

**2. `web/src/components/OfflineBanner.tsx`** — Amber banner below header:
- `useState(!navigator.onLine)` for initial state
- `useEffect` that adds `online`/`offline` event listeners on `window`, returns cleanup
- If online, return `null`
- Render: `<div role="status" aria-live="polite" className="fixed top-14 inset-x-0 z-30 bg-amber-50 border-b border-amber-200 text-amber-800 text-sm py-2 px-4 text-center">`
- Content: `{t('pwa.offlineBanner')}`
- No dismiss button — auto-hides when online

**3. `web/src/components/UpdateToast.tsx`** — PWA update prompt:
- Import `useRegisterSW` from `'virtual:pwa-register/react'`
- Destructure: `const { needRefresh: [needRefresh], updateServiceWorker } = useRegisterSW()`
- If `!needRefresh`, return `null`
- Render: `<div role="alert" aria-live="assertive" className="fixed top-16 start-1/2 -translate-x-1/2 z-50 bg-white border border-indigo-200 rounded-lg shadow-lg px-4 py-3 flex items-center gap-3">`
- Text: `{t('pwa.updateAvailable')}` in `text-sm text-gray-800`
- Refresh button: `onClick={() => updateServiceWorker(true)}` with `px-3 py-1 bg-indigo-600 text-white text-sm font-bold rounded-md`
- Later button: `onClick={() => updateServiceWorker(false)}` with `text-sm text-gray-500`
- CRITICAL: Use `updateServiceWorker(true)` to reload, NEVER `window.location.reload()` directly

**4. `web/src/components/InstallBanner.tsx`** — Android + iOS install:
- `type InstallMode = 'android' | 'ios' | 'none'`
- localStorage key: `'promptplay_install_dismissed'`
- `useEffect` on mount:
  - If localStorage has dismissed key, return early
  - If `window.matchMedia('(display-mode: standalone)').matches`, return early (already installed)
  - Check iOS: `/iPad|iPhone|iPod/.test(navigator.userAgent) && !(window as any).MSStream` — if true, `setMode('ios')`
  - Otherwise add `beforeinstallprompt` event listener, `e.preventDefault()`, store event in state, `setMode('android')`
  - Return cleanup removing event listener
- `dismiss()`: set localStorage key to `'1'`, set mode to `'none'`
- `install()`: call `deferredPrompt.prompt()`, await `deferredPrompt.userChoice`, then `dismiss()`
- If mode is `'none'`, return `null`
- Render: `<nav role="complementary" aria-label={t('pwa.installButton')} className="fixed bottom-14 lg:bottom-0 inset-x-0 z-30 bg-white border-t border-gray-200 shadow-lg px-4 py-3 flex items-center justify-between gap-3">`
- Text: `mode === 'ios' ? t('pwa.iosInstallHint') : t('pwa.installButton')` in `text-sm text-gray-800`
- Android install button: `h-11 px-4 bg-indigo-600 text-white text-sm font-bold rounded-md` (only when mode === 'android')
- Dismiss button: `h-11 px-3 text-sm text-gray-500` with `aria-label={t('pwa.dismiss')}`
- Touch targets: all buttons minimum h-11 (44px)

**5. `web/src/components/AppHead.tsx`** — Per-route helmet:
- Import `Helmet` from `'react-helmet-async'`
- Props: `{ title?: string; description?: string }`
- Compute `fullTitle`: if title provided, `${title} — PromptPlay`; else `'PromptPlay — Learn AI Prompting'`
- Render: `<Helmet><title>{fullTitle}</title>{description && <meta name="description" content={description} />}<meta property="og:title" content={fullTitle} /></Helmet>`
  </action>
  <verify>
    <automated>cd "D:\claude code\code leraning game\web" && npx tsc --noEmit 2>&1 | head -20 && test -f src/components/Sidebar.tsx && echo "Sidebar: exists" && test -f src/components/OfflineBanner.tsx && echo "OfflineBanner: exists" && test -f src/components/UpdateToast.tsx && echo "UpdateToast: exists" && test -f src/components/InstallBanner.tsx && echo "InstallBanner: exists" && test -f src/components/AppHead.tsx && echo "AppHead: exists"</automated>
  </verify>
  <acceptance_criteria>
    - web/src/components/Sidebar.tsx exports `Sidebar`, contains `hidden lg:flex`, contains `start-0`, contains `border-e`, contains `aria-label="Main navigation"`
    - web/src/components/OfflineBanner.tsx exports `OfflineBanner`, contains `role="status"`, contains `aria-live="polite"`, contains `navigator.onLine`
    - web/src/components/UpdateToast.tsx exports `UpdateToast`, contains `useRegisterSW`, contains `updateServiceWorker(true)`, contains `role="alert"`, does NOT contain `window.location.reload`
    - web/src/components/InstallBanner.tsx exports `InstallBanner`, contains `beforeinstallprompt`, contains `promptplay_install_dismissed`, contains `display-mode: standalone`, contains `role="complementary"`, contains `h-11`
    - web/src/components/AppHead.tsx exports `AppHead`, contains `Helmet` from `react-helmet-async`, contains `og:title`
    - `npx tsc --noEmit` passes with no errors
  </acceptance_criteria>
  <done>All 5 PWA UI components created with correct accessibility roles, CSS logical properties, i18n integration, and TypeScript types</done>
</task>

<task type="auto">
  <name>Task 2: Wire responsive layout shell and add per-route AppHead to all pages</name>
  <files>web/src/App.tsx, web/src/components/TabBar.tsx, web/src/pages/HomePage.tsx, web/src/pages/SkillTreePage.tsx, web/src/pages/ProfilePage.tsx, web/src/pages/LessonPage.tsx, web/src/pages/OnboardingPage.tsx</files>
  <read_first>
    - web/src/App.tsx (current layout to modify)
    - web/src/components/TabBar.tsx (add lg:hidden)
    - web/src/pages/HomePage.tsx (add AppHead)
    - web/src/pages/SkillTreePage.tsx (add AppHead)
    - web/src/pages/ProfilePage.tsx (add AppHead)
    - web/src/pages/LessonPage.tsx (add AppHead)
    - web/src/pages/OnboardingPage.tsx (add AppHead)
    - .planning/phases/14-pwa-web-polish/14-UI-SPEC.md (responsive layout contract, per-route titles table)
  </read_first>
  <action>
**1. Update `web/src/App.tsx`** — Restructure RootLayout for responsive desktop:

Add imports at top:
```typescript
import { HelmetProvider } from 'react-helmet-async'
import { Sidebar } from './components/Sidebar'
import { OfflineBanner } from './components/OfflineBanner'
import { UpdateToast } from './components/UpdateToast'
import { InstallBanner } from './components/InstallBanner'
```

Wrap the `RouterProvider` in `App()` with `HelmetProvider`:
```typescript
export function App() {
  return (
    <HelmetProvider>
      <RouterProvider router={router} />
    </HelmetProvider>
  )
}
```

Restructure `RootLayout` return JSX (after the hydration/onboarding guards remain unchanged):
```typescript
return (
  <div className="flex min-h-dvh">
    <Sidebar />
    <div className="flex flex-col flex-1 lg:ms-64">
      <GameHeader />
      <OfflineBanner />
      <main className="flex-1 flex flex-col pb-14 lg:pb-0">
        <Outlet />
      </main>
      <TabBar />
    </div>
    <UpdateToast />
    <InstallBanner />
    {pendingLevelUp !== null && (
      <LevelUpModal level={pendingLevelUp} onDismiss={clearPendingLevelUp} />
    )}
  </div>
)
```
Key changes from current:
- Outer div: `flex min-h-dvh` (was `flex flex-col min-h-dvh`)
- `<Sidebar />` as first child (renders nothing on mobile via `hidden lg:flex`)
- Content wrapper: `<div className="flex flex-col flex-1 lg:ms-64">` — uses logical margin-inline-start for RTL
- `<OfflineBanner />` between GameHeader and main
- main: add `lg:pb-0` (no bottom padding on desktop since no TabBar)
- `<UpdateToast />` and `<InstallBanner />` after TabBar (they are fixed-position, order doesn't matter for layout)

**2. Update `web/src/components/TabBar.tsx`** — Hide on desktop:

Change the `<nav>` className from:
`"fixed bottom-0 inset-x-0 h-14 bg-white border-t border-gray-200 flex items-stretch justify-around z-40"`
to:
`"fixed bottom-0 inset-x-0 h-14 bg-white border-t border-gray-200 flex items-stretch justify-around z-40 lg:hidden"`

Only change: append ` lg:hidden` to the className string.

**3. Add AppHead to each page component** — Import `AppHead` from `'@/components/AppHead'` and add as first child inside the page's outermost JSX element:

- `web/src/pages/HomePage.tsx`: `<AppHead title={undefined} description="Practice AI prompting with gamified lessons. Earn XP, build streaks." />` (title undefined = default "PromptPlay — Learn AI Prompting")
- `web/src/pages/SkillTreePage.tsx`: `<AppHead title="Skill Tree" />`
- `web/src/pages/ProfilePage.tsx`: `<AppHead title="Profile" />`
- `web/src/pages/LessonPage.tsx`: `<AppHead title={lessonTitle} />` where `lessonTitle` is derived from the current lesson data (use the lesson's title from content, e.g., `t(lesson.title)` or the localized string). If lesson data not yet loaded, use fallback "Lesson"
- `web/src/pages/OnboardingPage.tsx`: `<AppHead title="Start Learning" />`

Each AppHead must be the FIRST child element inside the page's return JSX (before any other content).
  </action>
  <verify>
    <automated>cd "D:\claude code\code leraning game\web" && npx tsc --noEmit 2>&1 | head -20 && grep -c "lg:hidden" src/components/TabBar.tsx && grep -c "HelmetProvider" src/App.tsx && grep -c "Sidebar" src/App.tsx && grep -c "OfflineBanner" src/App.tsx && grep -c "UpdateToast" src/App.tsx && grep -c "InstallBanner" src/App.tsx && grep -c "lg:ms-64" src/App.tsx && grep -c "lg:pb-0" src/App.tsx && grep -c "AppHead" src/pages/HomePage.tsx && grep -c "AppHead" src/pages/SkillTreePage.tsx && grep -c "AppHead" src/pages/ProfilePage.tsx && grep -c "AppHead" src/pages/LessonPage.tsx && grep -c "AppHead" src/pages/OnboardingPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - web/src/App.tsx contains `import { HelmetProvider } from 'react-helmet-async'`
    - web/src/App.tsx contains `<HelmetProvider>` wrapping RouterProvider
    - web/src/App.tsx contains `<Sidebar />` inside RootLayout
    - web/src/App.tsx contains `<OfflineBanner />` between GameHeader and main
    - web/src/App.tsx contains `<UpdateToast />` and `<InstallBanner />`
    - web/src/App.tsx contains `lg:ms-64` on the content wrapper div
    - web/src/App.tsx contains `lg:pb-0` on the main element
    - web/src/App.tsx RootLayout outer div has `flex min-h-dvh` (not flex-col)
    - web/src/components/TabBar.tsx nav element contains `lg:hidden`
    - web/src/pages/HomePage.tsx contains `<AppHead` with no title (uses default)
    - web/src/pages/SkillTreePage.tsx contains `<AppHead title="Skill Tree"`
    - web/src/pages/ProfilePage.tsx contains `<AppHead title="Profile"`
    - web/src/pages/LessonPage.tsx contains `<AppHead title=`
    - web/src/pages/OnboardingPage.tsx contains `<AppHead title="Start Learning"`
    - `npx tsc --noEmit` passes with no errors
  </acceptance_criteria>
  <done>Responsive layout complete: Sidebar shows at lg+, TabBar hides at lg+, content area uses lg:ms-64, all pages have per-route titles via AppHead, PWA banners (offline, update, install) are wired into layout</done>
</task>

</tasks>

<verification>
1. `cd web && npx tsc --noEmit` passes
2. `cd web && npx vite build` completes without errors
3. Built output contains service worker: `ls web/dist/sw.js` or similar
4. All 5 new components exist and export correctly
5. TabBar has `lg:hidden`, Sidebar has `hidden lg:flex`
6. App.tsx has HelmetProvider, Sidebar, OfflineBanner, UpdateToast, InstallBanner
7. All 5 page components have AppHead with appropriate titles
8. No physical left/right CSS in any new component (all logical properties)
</verification>

<success_criteria>
- Desktop viewport (1024px+): Sidebar visible on start-side, TabBar hidden, content offset by ms-64
- Mobile/tablet viewport: TabBar visible at bottom, no Sidebar
- OfflineBanner renders below GameHeader (top-14) with amber styling when offline
- UpdateToast renders centered at top with indigo border when SW update available
- InstallBanner renders above TabBar with iOS/Android-specific content
- All 5 pages have unique titles via AppHead + react-helmet-async
- RTL mode: Sidebar appears on end-side (right), content margin shifts correctly
- TypeScript compiles, Vite builds, no console errors
</success_criteria>

<output>
After completion, create `.planning/phases/14-pwa-web-polish/14-02-SUMMARY.md`
</output>
