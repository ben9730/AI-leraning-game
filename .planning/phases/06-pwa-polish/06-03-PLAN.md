---
phase: 06-pwa-polish
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - PromptPlay/src/features/gamification/celebrations/LessonCelebration.tsx
  - PromptPlay/src/features/gamification/celebrations/LevelUpModal.tsx
  - PromptPlay/src/features/pwa/__tests__/animation-audit.test.ts
  - PromptPlay/src/features/pwa/__tests__/responsive-layout.test.ts
  - PromptPlay/app/(tabs)/index.tsx
  - PromptPlay/app/(tabs)/skill-tree.tsx
  - PromptPlay/app/(tabs)/profile.tsx
autonomous: false
requirements: [PWA-03, PWA-04]

must_haves:
  truths:
    - "Celebration animations run at 60fps on a 2GB RAM Android device (only transform + opacity animated)"
    - "All lesson content and UI is readable on 320px-wide mobile viewports without horizontal scrolling"
    - "No animated style uses layout properties (width, height, top, left, marginLeft, paddingLeft)"
    - "Tab screens use responsive values that adapt from mobile to tablet"
  artifacts:
    - path: "PromptPlay/src/features/pwa/__tests__/animation-audit.test.ts"
      provides: "Static analysis test ensuring animated styles only use transform + opacity"
      contains: "transform"
    - path: "PromptPlay/src/features/pwa/__tests__/responsive-layout.test.ts"
      provides: "Test verifying no directional left/right style properties in tab screens"
      contains: "paddingLeft|paddingRight|marginLeft|marginRight"
  key_links:
    - from: "LessonCelebration.tsx"
      to: "useAnimatedStyle"
      via: "only transform + opacity properties"
      pattern: "useAnimatedStyle.*transform|opacity"
    - from: "LevelUpModal.tsx"
      to: "useAnimatedStyle"
      via: "only transform + opacity properties"
      pattern: "useAnimatedStyle.*transform|opacity"
---

<objective>
Audit and fix animation performance for low-end Android devices and ensure responsive layout works across all mobile viewports.

Purpose: PWA-03 requires mobile viewport readability. PWA-04 requires 60fps animations on 2GB RAM Android. These are the final production-hardening steps before the app ships.

Output: Optimized animation components using only transform/opacity, responsive layout fixes across all tab screens, and automated guard tests preventing performance regressions.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-pwa-polish/06-RESEARCH.md
@.planning/phases/03-gamification-engine/03-03-SUMMARY.md

<!-- Key animation files from Phase 3 -->
@PromptPlay/src/features/gamification/celebrations/LessonCelebration.tsx
@PromptPlay/src/features/gamification/celebrations/LevelUpModal.tsx

<!-- Tab screens to audit for responsive layout -->
@PromptPlay/app/(tabs)/index.tsx
@PromptPlay/app/(tabs)/skill-tree.tsx
@PromptPlay/app/(tabs)/profile.tsx

<interfaces>
<!-- Reanimated API patterns already in use -->
From react-native-reanimated:
```typescript
import { useSharedValue, useAnimatedStyle, withTiming, withSpring } from 'react-native-reanimated';
// RULE: Inside useAnimatedStyle, ONLY use transform and opacity
// NEVER: width, height, top, left, margin*, padding* (triggers layout recalc every frame)
```

<!-- RTL conventions from CLAUDE.md -->
RTL-safe style properties:
```typescript
// USE: paddingStart, paddingEnd, marginStart, marginEnd, start, end
// NEVER: paddingLeft, paddingRight, marginLeft, marginRight, left, right
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Animation performance audit + fix celebration components</name>
  <files>
    PromptPlay/src/features/gamification/celebrations/LessonCelebration.tsx,
    PromptPlay/src/features/gamification/celebrations/LevelUpModal.tsx,
    PromptPlay/src/features/pwa/__tests__/animation-audit.test.ts
  </files>
  <action>
1. Read and audit `LessonCelebration.tsx` and `LevelUpModal.tsx` for animation anti-patterns:
   - Check every `useAnimatedStyle` callback for layout properties: width, height, top, left, right, bottom, margin*, padding*
   - Check for Zustand store reads inside worklets (must use useSharedValue instead — per research Pitfall 5)
   - Check that animated elements count is under 15 per screen (per research recommendation)
   - Check for `withDelay` chains that might stack — prefer `withSequence` for chained animations

2. Fix any animation anti-patterns found:
   - Replace any layout property animations with equivalent transform animations:
     - `width` changes -> `transform: [{ scaleX }]`
     - `height` changes -> `transform: [{ scaleY }]`
     - `top/left` changes -> `transform: [{ translateX }, { translateY }]`
     - `marginTop` changes -> `transform: [{ translateY }]`
   - If any Zustand values are read inside worklets, extract them to `useSharedValue` outside the worklet and pass in
   - Ensure all Lottie animations have `autoPlay` and explicit `speed` props (avoid unbounded render loops)
   - Add `renderMode="HARDWARE"` to Lottie components if available (enables hardware-accelerated rendering on Android)

3. Create `PromptPlay/src/features/pwa/__tests__/animation-audit.test.ts`:
   - This is a static analysis test — reads source files and checks for anti-patterns
   - Read LessonCelebration.tsx and LevelUpModal.tsx file contents using `fs.readFileSync`
   - Test 1 ("no layout properties in animated styles"): Regex match inside `useAnimatedStyle` blocks for forbidden properties: `/\b(width|height|top|left|right|bottom|marginTop|marginBottom|marginLeft|marginRight|paddingTop|paddingBottom|paddingLeft|paddingRight)\s*:/`. Should find ZERO matches.
   - Test 2 ("no store reads inside worklets"): Check for `useProgressStore` or `getState()` inside `useAnimatedStyle` or `runOnUI` blocks. Should find ZERO matches.
   - Test 3 ("animated element count under limit"): Count `Animated.View` and `Animated.Text` occurrences in each file. Each file should have fewer than 15.
   - Use the `pwa` jest project (node or jsdom environment — these are pure file reads)

4. Performance optimization notes for the executor:
   - Reanimated worklets already run on the UI thread (per research) — the main risk is layout thrashing, not thread bridging
   - `withSpring` is heavier than `withTiming` — prefer `withTiming` with easing for celebratory effects where spring physics aren't needed
   - Cap Lottie animation duration: set `loop={false}` on one-shot celebrations to avoid continuous GPU work
  </action>
  <verify>
    <automated>cd "D:/claude code/code leraning game/PromptPlay" && npx jest --testPathPattern="animation-audit" --no-coverage</automated>
  </verify>
  <done>
    - LessonCelebration.tsx and LevelUpModal.tsx use ONLY transform + opacity in animated styles
    - No Zustand store reads inside worklets
    - Animated element count under 15 per component
    - Animation audit tests pass and will catch future regressions
  </done>
</task>

<task type="auto">
  <name>Task 2: Responsive layout audit + fixes for mobile viewports</name>
  <files>
    PromptPlay/src/features/pwa/__tests__/responsive-layout.test.ts,
    PromptPlay/app/(tabs)/index.tsx,
    PromptPlay/app/(tabs)/skill-tree.tsx,
    PromptPlay/app/(tabs)/profile.tsx
  </files>
  <action>
1. Read and audit all three tab screens + key components for responsive issues:
   - Check for hardcoded pixel widths that would overflow on 320px viewport (iPhone SE)
   - Check for `paddingLeft`/`paddingRight`/`marginLeft`/`marginRight` (RTL violations — should be Start/End)
   - Check for `left`/`right` positioning (should be `start`/`end`)
   - Check for text that might overflow without `flexShrink: 1` or `numberOfLines`
   - Check for horizontal ScrollView content that might not fit on small screens

2. Fix responsive issues found:
   - Replace any hardcoded widths > 300px with percentage-based or flex-based layouts
   - Replace any remaining directional `left/right` properties with `start/end`
   - Add `flexShrink: 1` to text containers that might overflow
   - Ensure lesson content area uses `paddingHorizontal` (which is Start/End-safe in RN) or explicit `paddingStart`/`paddingEnd`
   - For skill tree nodes: if they use fixed widths, ensure they fit in a 320px viewport with padding
   - For profile tab: ensure badge grid wraps properly on narrow screens (use `flexWrap: 'wrap'`)

3. Also audit these key feature components (read-only, fix if needed):
   - `PromptPlay/src/features/exercise/ExerciseRunner.tsx` — exercise cards must not overflow
   - `PromptPlay/src/features/lesson/LessonContentScreen.tsx` — lesson text must wrap
   - `PromptPlay/src/features/skill-tree/SkillTreeNode.tsx` — nodes must fit mobile width
   - `PromptPlay/src/features/gamification/streakDisplay.tsx` — streak badge must not overflow

4. Create `PromptPlay/src/features/pwa/__tests__/responsive-layout.test.ts`:
   - Static analysis test reading source files
   - Test 1 ("no directional left/right style properties in tab screens"): Read index.tsx, skill-tree.tsx, profile.tsx. Regex for style objects containing `paddingLeft`, `paddingRight`, `marginLeft`, `marginRight`, `left:`, `right:` (excluding `borderTopLeftRadius` and similar compound properties, and excluding comments). Should find ZERO matches in style objects.
   - Test 2 ("no hardcoded widths over 300px"): Regex for `width:\s*\d+` where the number > 300 in tab screen files. Should find ZERO matches.
   - Test 3 ("exercise components use RTL-safe properties"): Read ExerciseRunner.tsx and check for directional violations.
   - Use the `pwa` jest project
  </action>
  <verify>
    <automated>cd "D:/claude code/code leraning game/PromptPlay" && npx jest --testPathPattern="responsive-layout|animation-audit" --no-coverage</automated>
  </verify>
  <done>
    - No directional left/right style properties in tab screens or exercise components
    - No hardcoded widths that overflow on 320px viewport
    - All tab screens use flex-based or percentage-based responsive layouts
    - Animation and responsive layout audit tests pass
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Animation performance optimizations (transform/opacity only) and responsive layout fixes across all tab screens and key components. Automated regression tests guard against future violations.
  </what-built>
  <how-to-verify>
    1. Run `cd PromptPlay && npx expo start --web` and open in Chrome
    2. Open Chrome DevTools -> toggle device toolbar -> select "iPhone SE" (320px width)
    3. Navigate through all three tabs (Home, Skill Tree, Profile) — verify no horizontal scrolling and all content is readable
    4. Open a lesson and complete an exercise — verify the celebration animation plays smoothly
    5. Open Chrome DevTools -> Performance tab -> record while a celebration animation plays -> verify no layout shift warnings and frame times are under 16ms (60fps)
    6. If you have access to a low-end Android device or Chrome's CPU throttling (4x slowdown), verify animations still feel smooth
  </how-to-verify>
  <resume-signal>Type "approved" if animations are smooth and layouts are responsive, or describe specific issues</resume-signal>
</task>

</tasks>

<verification>
1. `cd PromptPlay && npx jest --testPathPattern="animation-audit|responsive-layout" --no-coverage` — all audit tests pass
2. No `useAnimatedStyle` callback contains layout properties (width, height, top, left, margin*, padding*)
3. All tab screens use RTL-safe directional properties (Start/End, not Left/Right)
4. Manual verification: animations smooth at 60fps, no horizontal scrolling on 320px viewport
</verification>

<success_criteria>
- Animation audit test confirms: zero layout properties in animated styles, zero store reads in worklets, under 15 animated elements per component
- Responsive layout test confirms: zero directional left/right properties, zero hardcoded widths over 300px
- Human verification: smooth 60fps animations and readable content on 320px mobile viewport
</success_criteria>

<output>
After completion, create `.planning/phases/06-pwa-polish/06-03-SUMMARY.md`
</output>
