---
phase: 03-gamification-engine
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - PromptPlay/src/features/gamification/badges.ts
  - PromptPlay/src/features/gamification/badges.test.ts
  - PromptPlay/src/features/gamification/celebrations/LessonCelebration.tsx
  - PromptPlay/src/features/gamification/celebrations/LevelUpModal.tsx
  - PromptPlay/src/features/gamification/tone.test.ts
  - PromptPlay/src/features/lesson/LessonCompletionScreen.tsx
  - PromptPlay/src/features/lesson/LessonScreen.tsx
  - PromptPlay/app/(tabs)/profile.tsx
  - PromptPlay/src/i18n/en/common.json
  - PromptPlay/src/i18n/he/common.json
  - PromptPlay/assets/lottie/celebration.json
  - PromptPlay/assets/lottie/level-up.json
autonomous: false
requirements: [GAME-05, GAME-06, GAME-09]

must_haves:
  truths:
    - "Completing a lesson plays a Lottie celebration animation"
    - "Level-up triggers a full-screen celebration modal with Lottie + haptics"
    - "At least 3 badges are earnable: first lesson, 7-day streak, chapter 1 complete"
    - "Badges display in the Profile tab with earned/unearned states"
    - "Badges are derived from store state — never stored directly"
    - "No guilt-framing text exists anywhere in gamification copy"
    - "pendingLevelUp is cleared after the level-up modal is dismissed"
  artifacts:
    - path: "PromptPlay/src/features/gamification/badges.ts"
      provides: "deriveBadges pure function and Badge type"
      exports: ["deriveBadges", "Badge", "BADGE_DEFINITIONS"]
    - path: "PromptPlay/src/features/gamification/celebrations/LessonCelebration.tsx"
      provides: "Lottie confetti overlay on lesson completion"
      exports: ["LessonCelebration"]
    - path: "PromptPlay/src/features/gamification/celebrations/LevelUpModal.tsx"
      provides: "Full-screen level-up celebration with Lottie + haptics"
      exports: ["LevelUpModal"]
    - path: "PromptPlay/app/(tabs)/profile.tsx"
      provides: "Badge grid display with earned/unearned states"
      contains: "deriveBadges"
  key_links:
    - from: "PromptPlay/src/features/lesson/LessonCompletionScreen.tsx"
      to: "PromptPlay/src/features/gamification/celebrations/LessonCelebration.tsx"
      via: "renders LessonCelebration overlay"
      pattern: "LessonCelebration"
    - from: "PromptPlay/src/features/lesson/LessonScreen.tsx"
      to: "PromptPlay/src/features/gamification/celebrations/LevelUpModal.tsx"
      via: "renders LevelUpModal when pendingLevelUp is set"
      pattern: "LevelUpModal"
    - from: "PromptPlay/app/(tabs)/profile.tsx"
      to: "PromptPlay/src/features/gamification/badges.ts"
      via: "calls deriveBadges with store state"
      pattern: "deriveBadges"
    - from: "PromptPlay/src/features/gamification/celebrations/LevelUpModal.tsx"
      to: "PromptPlay/src/store/useProgressStore.ts"
      via: "calls clearPendingLevelUp on dismiss"
      pattern: "clearPendingLevelUp"
---

<objective>
Add badges (derived from state), Lottie celebration animations, level-up modal, and wire everything into the lesson flow and profile screen.

Purpose: Celebrations provide the dopamine reward loop that makes completing lessons feel rewarding. Badges give long-term visible progress markers. This plan is the "feel good" layer — the engine math from plans 01 and 02 becomes tangible visual/haptic feedback here.

Output: LessonCelebration animation, LevelUpModal, deriveBadges system, profile badge display, all wired into the lesson flow.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-gamification-engine/03-RESEARCH.md
@.planning/phases/03-gamification-engine/03-01-SUMMARY.md
@.planning/phases/03-gamification-engine/03-02-SUMMARY.md

<interfaces>
<!-- Contracts from Plans 03-01 and 03-02 that this plan depends on. -->

From PromptPlay/src/features/gamification/engine.ts:
```typescript
export interface XPResult {
  base: number
  streakMultiplier: number
  perfectionBonus: number
  total: number
}
export function calcXP(lessonBaseXP: number, streakCount: number, isPerfect: boolean): XPResult
export function calcStreakUpdate(lastActivityDate: string, streakCount: number, streakFreezes: number, todayISO: string): StreakResult
```

From PromptPlay/src/store/types.ts:
```typescript
export interface UserProgress {
  xpTotal: number
  streakCount: number
  completedLessons: string[]
  peakStreak: number
  pendingLevelUp: number | null
  streakFreezeUsedEver: boolean
  // ... actions ...
  clearPendingLevelUp: () => void
}
export const getLevel: (xp: number) => number
```

From PromptPlay/src/features/gamification/constants.ts:
```typescript
export const LEVEL_THRESHOLDS: number[]
export const DAILY_GOAL_XP: Record<'casual' | 'regular' | 'serious', number>
```

From PromptPlay/src/content/schema.ts:
```typescript
export interface Lesson {
  id: string; chapter: number; xpReward: number; /* ... */
}
```

From PromptPlay/src/features/lesson/LessonCompletionScreen.tsx:
```typescript
interface LessonCompletionScreenProps {
  lesson: Lesson
  totalScore: number
  onFinish: () => void
}
```

From PromptPlay/src/features/lesson/LessonScreen.tsx (updated in 03-01):
- handleFinish calls calcXP, addXP, updateStreak, completeLesson, unlockLesson
- Passes xpResult to LessonCompletionScreen
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Badge derivation system and tone audit test</name>
  <files>
    PromptPlay/src/features/gamification/badges.ts,
    PromptPlay/src/features/gamification/badges.test.ts,
    PromptPlay/src/features/gamification/tone.test.ts,
    PromptPlay/src/i18n/en/common.json,
    PromptPlay/src/i18n/he/common.json
  </files>
  <behavior>
    - deriveBadges with 0 completed lessons => first_lesson.earned = false
    - deriveBadges with 1+ completed lessons => first_lesson.earned = true
    - deriveBadges with peakStreak < 7 => streak_7.earned = false
    - deriveBadges with peakStreak >= 7 => streak_7.earned = true
    - deriveBadges with all chapter 1 lesson IDs completed => chapter_1_complete.earned = true
    - deriveBadges with xpTotal giving level >= 3 => level_3.earned = true
    - deriveBadges with streakFreezeUsedEver = true => resilient.earned = true
    - Badge derivation uses peakStreak (not streakCount) for streak badge
    - At least 5 badges defined total
    - Tone test: no i18n values contain guilt-framing words (lose, lost, miss, fail, die, break, punish, shame)
  </behavior>
  <action>
    1. Create `PromptPlay/src/features/gamification/badges.ts`:
       - Export `Badge` interface: `{ id: string, titleKey: string, descriptionKey: string, icon: string, earned: boolean }`
       - Export `CHAPTER_1_LESSON_IDS: string[]` — the 5 lesson IDs that comprise chapter 1. Read from the content/lessons directory structure: `['lesson-01-what-is-prompting', 'lesson-02-clarity', 'lesson-03-specificity', 'lesson-04-context', 'lesson-05-intent']`
       - Export `deriveBadges(completedLessons: string[], peakStreak: number, xpTotal: number, streakFreezeUsedEver: boolean): Badge[]`
       - 5 badges:
         1. `first_lesson` — earned when completedLessons.length >= 1 — icon: "🎯"
         2. `streak_7` — earned when peakStreak >= 7 — icon: "🔥"
         3. `chapter_1_complete` — earned when all CHAPTER_1_LESSON_IDS are in completedLessons — icon: "📚"
         4. `level_3` — earned when getLevel(xpTotal) >= 3 — icon: "⭐"
         5. `resilient` — earned when streakFreezeUsedEver is true — icon: "🛡️"
       - Import `getLevel` from `@/src/store/types`
       - CRITICAL: Badges are NEVER stored. Always computed from current state on every call.

    2. Create tests FIRST in `PromptPlay/src/features/gamification/badges.test.ts`:
       - Test each badge's earned condition independently
       - Test that exactly 5 badges are returned
       - Test that a brand new user (empty state) has 0 earned badges
       - Test that peakStreak is used (not some other field) for streak_7

    3. Create `PromptPlay/src/features/gamification/tone.test.ts`:
       - Load English and Hebrew i18n JSON files
       - Recursively scan all string values
       - Assert NONE contain the words: "lose", "lost", "miss", "missed", "fail", "failed", "die", "dying", "break", "breaking", "punish", "shame", "guilt"
       - This is a safeguard for GAME-09 (no guilt-based mechanics)
       - Note: scan only gamification-related and streak-related keys, not exercise feedback keys (which legitimately use "failed" for wrong answers)
         - Specifically scan keys under: "gamification.*", "streak.*", "badge.*", "level.*"

    4. Add badge i18n keys to `PromptPlay/src/i18n/en/common.json`:
       ```json
       "badge": {
         "first_lesson": { "title": "First Steps", "description": "Complete your first lesson" },
         "streak_7": { "title": "On Fire", "description": "Reach a 7-day streak" },
         "chapter_1": { "title": "Chapter Champion", "description": "Complete all Chapter 1 lessons" },
         "level_3": { "title": "Rising Star", "description": "Reach Level 3" },
         "resilient": { "title": "Resilient", "description": "Use a streak freeze and keep going" },
         "earned": "Earned!",
         "locked": "Keep going to earn this badge"
       }
       ```

    5. Add badge i18n keys to `PromptPlay/src/i18n/he/common.json`:
       ```json
       "badge": {
         "first_lesson": { "title": "צעדים ראשונים", "description": "השלימו את השיעור הראשון" },
         "streak_7": { "title": "בלהבות", "description": "הגיעו לרצף של 7 ימים" },
         "chapter_1": { "title": "אלוף הפרק", "description": "השלימו את כל שיעורי פרק 1" },
         "level_3": { "title": "כוכב עולה", "description": "הגיעו לרמה 3" },
         "resilient": { "title": "עמיד", "description": "השתמשו בהקפאת רצף והמשיכו" },
         "earned": "הושג!",
         "locked": "המשיכו כדי להשיג את התג הזה"
       }
       ```
  </action>
  <verify>
    <automated>cd PromptPlay && npx jest src/features/gamification --testPathPattern="(badges|tone)" --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <done>
    - deriveBadges returns 5 badges with correct earned conditions
    - Badge derivation is pure (no store dependency, takes params)
    - Tone test passes — no guilt-framing in gamification i18n keys
    - Badge i18n keys exist in both en and he
    - All tests green
  </done>
</task>

<task type="auto">
  <name>Task 2: Celebration animations, level-up modal, profile badges, and wiring</name>
  <files>
    PromptPlay/src/features/gamification/celebrations/LessonCelebration.tsx,
    PromptPlay/src/features/gamification/celebrations/LevelUpModal.tsx,
    PromptPlay/src/features/lesson/LessonCompletionScreen.tsx,
    PromptPlay/src/features/lesson/LessonScreen.tsx,
    PromptPlay/app/(tabs)/profile.tsx,
    PromptPlay/assets/lottie/celebration.json,
    PromptPlay/assets/lottie/level-up.json
  </files>
  <action>
    1. Install lottie-react-native (MUST use expo install, not npm):
       ```bash
       cd PromptPlay && npx expo install lottie-react-native
       ```

    2. Add Lottie animation assets:
       - Create `PromptPlay/assets/lottie/` directory
       - Download a free celebration/confetti Lottie JSON from LottieFiles (CC0 or "Free for commercial use" license). Keep under 100KB.
       - Download a free level-up/star Lottie JSON. Keep under 100KB.
       - If downloading is not feasible in the executor environment, create minimal placeholder JSON files that LottieView can load without crashing:
         ```json
         {"v":"5.5.7","fr":30,"ip":0,"op":60,"w":300,"h":300,"nm":"placeholder","layers":[]}
         ```
       - The placeholder approach is acceptable for v1 — real assets can be swapped in later.

    3. Create `PromptPlay/src/features/gamification/celebrations/LessonCelebration.tsx`:
       - Import `LottieView` from `lottie-react-native`
       - Import `Haptics` from `expo-haptics`
       - Props: `{ visible: boolean; onFinish: () => void }`
       - When visible:
         - Play Lottie confetti animation (autoPlay, loop=false)
         - Fire `Haptics.notificationAsync(Haptics.NotificationFeedbackType.Success)` on mount
         - Call onFinish when animation completes (`onAnimationFinish`)
       - Render as an absolute-positioned overlay on top of the completion screen
       - Style: position absolute, fill parent, pointerEvents 'none' so user can still tap Continue underneath

    4. Create `PromptPlay/src/features/gamification/celebrations/LevelUpModal.tsx`:
       - Import `LottieView` from `lottie-react-native`
       - Import `Haptics` from `expo-haptics`
       - Import `useTranslation` for i18n
       - Props: `{ level: number; onDismiss: () => void }`
       - Full-screen modal overlay (semi-transparent background):
         - Lottie level-up animation (autoPlay, loop=false)
         - "Level Up!" title text (from i18n: `gamification.level_up`)
         - "You reached Level {N}!" subtitle (from i18n: `gamification.level_up_message`)
         - "Continue" button that calls onDismiss
       - On mount: fire `Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Heavy)`
       - CRITICAL: onDismiss MUST call `clearPendingLevelUp()` — if not cleared, modal fires again on next app open. The parent (LessonScreen) handles this.
       - RTL-safe: use `paddingStart/End`, text alignment based on `isRTL()`

    5. Update `PromptPlay/src/features/lesson/LessonCompletionScreen.tsx`:
       - Add `LessonCelebration` overlay that plays on mount
       - Add optional `xpBreakdown?: { base: number; streakMultiplier: number; perfectionBonus: number; total: number }` prop
       - If xpBreakdown is provided, display XP breakdown:
         - Base XP: "+{base} XP"
         - If streakMultiplier > 1: "Streak bonus: x{multiplier}"
         - If perfectionBonus > 0: "Perfection bonus: +{bonus} XP"
       - Replace the static `lesson.xpReward` display with `xpBreakdown.total` if available
       - The celebration animation auto-plays once on mount; does not block the Continue button

    6. Update `PromptPlay/src/features/lesson/LessonScreen.tsx`:
       - Import `LevelUpModal` and `useProgressStore` selector for `pendingLevelUp` and `clearPendingLevelUp`
       - After lesson completion (when step.phase === 'complete'), check if `pendingLevelUp` is set
       - If set, render `LevelUpModal` AFTER the user taps Continue on LessonCompletionScreen (not during):
         - Add a local state `showLevelUp` that is set when the user taps Continue AND pendingLevelUp exists
         - The flow: completion screen -> user taps Continue -> if pendingLevelUp, show LevelUpModal -> user taps Continue on modal -> clearPendingLevelUp + navigate home
         - If no pendingLevelUp, navigate home directly (existing behavior)

    7. Update `PromptPlay/app/(tabs)/profile.tsx`:
       - Import `deriveBadges` from `@/src/features/gamification/badges`
       - Import `useProgressStore` to read completedLessons, peakStreak, xpTotal, streakFreezeUsedEver
       - Import `getLevel` from `@/src/store/types`
       - Compute badges: `const badges = deriveBadges(completedLessons, peakStreak, xpTotal, streakFreezeUsedEver)`
       - Render a badge grid below existing profile content:
         - Section title: "Achievements" / "הישגים"
         - Grid layout (2 columns on mobile)
         - Each badge card shows: icon (emoji), title (from i18n), description (from i18n)
         - Earned badges: full color, "Earned!" label
         - Unearned badges: greyed out / dimmed, "Keep going to earn this badge" label
       - Also display: Level, Total XP, Streak count, Daily Goal
       - RTL-safe layout throughout

    8. Update Jest config if needed — add lottie-react-native to transformIgnorePatterns in the react-native project. Also add a `__mocks__/lottie-react-native.js` mock:
       ```javascript
       const React = require('react');
       module.exports = React.forwardRef((props, ref) =>
         React.createElement('LottieView', { ...props, ref })
       );
       module.exports.default = module.exports;
       ```
  </action>
  <verify>
    <automated>cd PromptPlay && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>
    - LessonCelebration renders Lottie confetti overlay on lesson completion
    - LevelUpModal renders full-screen celebration with level number, Lottie, and haptics
    - LevelUpModal calls clearPendingLevelUp on dismiss (prevents re-fire)
    - LessonCompletionScreen shows XP breakdown (base, streak bonus, perfection bonus)
    - Profile tab displays badge grid with 5 badges, earned/unearned states
    - Badges derived from store state on every render (never stored)
    - lottie-react-native installed via npx expo install
    - TypeScript compiles without errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete gamification engine: XP with streak multiplier and perfection bonus, daily streak with freeze mechanics, level-up detection, 5 milestone badges, Lottie celebration on lesson completion, full-screen level-up modal, badge display in Profile tab.</what-built>
  <how-to-verify>
    1. Open the app and complete a lesson
       - Expected: Lottie confetti animation plays on the completion screen
       - Expected: XP breakdown shows base XP (and streak bonus if streak > 2)
    2. Check the home tab
       - Expected: Flame icon with streak count visible
    3. Complete enough lessons to cross a level threshold (or temporarily lower LEVEL_THRESHOLDS[1] to 20 for testing)
       - Expected: Full-screen level-up modal appears after tapping Continue on completion screen
       - Expected: Haptic feedback fires on level-up
       - Expected: Tapping Continue on level-up modal dismisses it and navigates home
       - Expected: Re-opening the app does NOT show the level-up modal again
    4. Navigate to Profile tab
       - Expected: Badge grid visible with 5 badges
       - Expected: "First Steps" badge shows as earned (since you completed a lesson)
       - Expected: Other badges show as locked/greyed out with encouraging description
    5. Check all visible text for tone
       - Expected: No "You lost your streak", "Don't miss a day", or similar guilt language
       - Expected: All text is encouraging: "Keep going!", "Great progress!", etc.
    6. Test in Hebrew locale
       - Expected: All gamification text renders in Hebrew with correct RTL layout
       - Expected: Badge descriptions are in Hebrew
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. `cd PromptPlay && npx jest src/features/gamification --no-coverage` — all gamification tests green (engine, badges, tone)
2. `cd PromptPlay && npx tsc --noEmit` — no type errors
3. `cd PromptPlay && npx jest --no-coverage` — full suite green (no regressions)
4. Visual: Lottie celebration plays on lesson completion
5. Visual: Level-up modal appears on level threshold crossing
6. Visual: Profile shows 5 badges with earned/unearned states
7. No guilt-framing text in any gamification or streak i18n key (automated tone test)
</verification>

<success_criteria>
- 5 badges derived from state (first lesson, 7-day streak, chapter 1, level 3, resilient)
- Lottie celebration animation on lesson completion (not every exercise)
- Full-screen level-up modal with Lottie + haptics on level-up
- pendingLevelUp cleared after modal dismiss (no re-fire bug)
- Profile tab shows badge grid with earned/unearned visual distinction
- All text in en and he, RTL-safe layouts throughout
- Tone test passes: no guilt-framing anywhere in gamification copy
- lottie-react-native installed via npx expo install
</success_criteria>

<output>
After completion, create `.planning/phases/03-gamification-engine/03-03-SUMMARY.md`
</output>
