---
phase: 04-skill-tree-onboarding
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - PromptPlay/app/_layout.tsx
  - PromptPlay/app/(onboarding)/_layout.tsx
  - PromptPlay/app/(onboarding)/welcome.tsx
  - PromptPlay/src/features/onboarding/GoalSelector.tsx
  - PromptPlay/src/features/onboarding/AccountPromptModal.tsx
  - PromptPlay/src/features/lesson/LessonScreen.tsx
  - PromptPlay/src/i18n/en/common.json
  - PromptPlay/src/i18n/he/common.json
autonomous: true
requirements:
  - ONBR-01
  - ONBR-02
  - ONBR-03
  - ONBR-04

must_haves:
  truths:
    - "New user (dailyGoal === null) is redirected to onboarding welcome screen before seeing tabs"
    - "Returning user (dailyGoal is set) sees tabs immediately with no onboarding flash"
    - "Goal selection writes to store and immediately navigates to lesson 1"
    - "User reaches first exercise within 60 seconds of opening (goal screen -> lesson 1)"
    - "Account prompt modal appears after completing lesson 2 (completedLessons.length >= 2), not before"
    - "Account prompt is skippable — user can dismiss and continue without signing up"
    - "No signup wall exists — entire app is usable without an account"
  artifacts:
    - path: "PromptPlay/app/(onboarding)/_layout.tsx"
      provides: "Onboarding route group stack navigator"
      min_lines: 10
    - path: "PromptPlay/app/(onboarding)/welcome.tsx"
      provides: "Goal selection screen — first screen new users see"
      min_lines: 30
    - path: "PromptPlay/src/features/onboarding/GoalSelector.tsx"
      provides: "Casual/Regular/Serious goal picker component"
      exports: ["GoalSelector"]
    - path: "PromptPlay/src/features/onboarding/AccountPromptModal.tsx"
      provides: "Deferred account creation modal"
      exports: ["AccountPromptModal"]
  key_links:
    - from: "PromptPlay/app/_layout.tsx"
      to: "PromptPlay/app/(onboarding)/welcome.tsx"
      via: "Redirect when dailyGoal === null"
      pattern: "Redirect.*onboarding"
    - from: "PromptPlay/app/(onboarding)/welcome.tsx"
      to: "PromptPlay/app/(lesson)/[lessonId].tsx"
      via: "router.replace after goal selection"
      pattern: "router\\.replace.*lesson-01"
    - from: "PromptPlay/src/features/lesson/LessonScreen.tsx"
      to: "PromptPlay/src/features/onboarding/AccountPromptModal.tsx"
      via: "Shown on completion when completedLessons.length >= 2 and no session"
      pattern: "AccountPromptModal|showAccountPrompt"
---

<objective>
Create the friction-free onboarding flow that gets new users into their first exercise within 60 seconds and defers account creation until after lesson 2.

Purpose: Remove all barriers to learning. Users should experience value before being asked for anything.
Output: Onboarding route group with goal selection, immediate lesson 1 entry, and a deferred account prompt modal.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-skill-tree-onboarding/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From PromptPlay/app/_layout.tsx (WILL BE MODIFIED):
```typescript
// Current root layout: Stack with (tabs), (lesson), modal
// Already imports useHasHydrated, gates on !loaded || !hasHydrated
// Need to add: dailyGoal check + Redirect to (onboarding)
// Need to add: (onboarding) Stack.Screen entry
export const unstable_settings = {
  initialRouteName: '(tabs)',
}
```

From PromptPlay/src/store/useProgressStore.ts:
```typescript
// Relevant state and actions:
dailyGoal: null,  // null = never set = trigger onboarding
setDailyGoal: (goal: 'casual' | 'regular' | 'serious' | null) => void
completedLessons: [] as string[]
// useHasHydrated() hook prevents flash
```

From PromptPlay/src/features/lesson/LessonScreen.tsx:
```typescript
// handleFinish() is called when user taps Continue on completion screen
// Currently: completeLesson, addXP, updateStreak, unlockLesson, then router.replace('/(tabs)')
// Need to add: show AccountPromptModal before navigating away (when conditions met)
```

From PromptPlay/src/i18n/en/common.json and he/common.json:
```
// Existing keys: tabs.home, tabs.skillTree, tabs.profile, lesson.*, gamification.*
// Need to add: onboarding.* keys and accountPrompt.* keys
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Onboarding route group + root layout redirect</name>
  <files>
    PromptPlay/app/(onboarding)/_layout.tsx,
    PromptPlay/app/(onboarding)/welcome.tsx,
    PromptPlay/src/features/onboarding/GoalSelector.tsx,
    PromptPlay/app/_layout.tsx,
    PromptPlay/src/i18n/en/common.json,
    PromptPlay/src/i18n/he/common.json
  </files>
  <action>
    **1. Create `PromptPlay/app/(onboarding)/_layout.tsx`:**
    - Simple Stack navigator with `headerShown: false`
    - Single screen: `welcome`

    **2. Create `PromptPlay/src/features/onboarding/GoalSelector.tsx`:**
    - Props: `{ onSelect: (goal: 'casual' | 'regular' | 'serious') => void }`
    - Renders three styled buttons for casual/regular/serious
    - Each button shows: goal label + daily XP target (10/20/30 XP from DAILY_GOAL_XP constants)
    - Import `DAILY_GOAL_XP` from `@/src/features/gamification/constants`
    - RTL: `paddingStart/End`, `textAlign` from `isRTL()`
    - Encouraging tone: heading like "How often do you want to learn?" not "Set your commitment level"
    - Use `useTranslation()` for all text

    **3. Create `PromptPlay/app/(onboarding)/welcome.tsx`:**
    - Import `GoalSelector` and `useProgressStore`
    - On goal select: call `setDailyGoal(goal)`, then `router.replace('/(lesson)/lesson-01-what-is-prompting')` to go directly to lesson 1
    - Use `router.replace` (not push) so back button does not return to onboarding
    - Layout: centered content, app logo/title area at top, GoalSelector below
    - No account creation mention anywhere on this screen (ONBR-01)

    **4. Modify `PromptPlay/app/_layout.tsx`:**
    - Add import: `import { Redirect } from 'expo-router'`
    - Add import: `import { useProgressStore } from '@/src/store/useProgressStore'`
    - In `RootLayout`, after hydration check, read `dailyGoal` from store
    - If `dailyGoal === null`, return `<Redirect href="/(onboarding)/welcome" />`
    - In `RootLayoutNav`, add `<Stack.Screen name="(onboarding)" options={{ headerShown: false }} />` BEFORE the (tabs) screen
    - Keep existing `unstable_settings.initialRouteName = '(tabs)'` so returning users go to tabs

    **5. Add i18n keys** to both `en/common.json` and `he/common.json`:
    ```
    "onboarding.goal.heading": "How often do you want to learn?" / "כמה פעמים תרצו ללמוד?"
    "onboarding.goal.casual": "Casual" / "קליל"
    "onboarding.goal.regular": "Regular" / "רגיל"
    "onboarding.goal.serious": "Serious" / "אינטנסיבי"
    "onboarding.goal.xpPerDay": "{{xp}} XP per day" / "{{xp}} XP ליום"
    "onboarding.welcome.title": "PromptPlay" / "PromptPlay"
    "onboarding.welcome.subtitle": "Learn AI prompting through play" / "למדו פרומפטינג דרך משחק"
    ```

    **CRITICAL:** The redirect must be gated on `useHasHydrated()` returning true. The existing `if (!loaded || !hasHydrated) return null` already handles this. Add the dailyGoal check AFTER this guard, before `return <RootLayoutNav />`.
  </action>
  <verify>
    <automated>cd PromptPlay && npx expo export --platform web 2>&1 | tail -5</automated>
  </verify>
  <done>
    - New user (dailyGoal === null) is redirected to welcome screen
    - Returning user (dailyGoal set) goes straight to tabs
    - Goal selection sets dailyGoal in store and navigates to lesson 1
    - No onboarding flash on returning users (hydration guard works)
    - All text is translated (en + he)
  </done>
</task>

<task type="auto">
  <name>Task 2: Deferred account prompt modal after lesson 2</name>
  <files>
    PromptPlay/src/features/onboarding/AccountPromptModal.tsx,
    PromptPlay/src/features/lesson/LessonScreen.tsx,
    PromptPlay/src/i18n/en/common.json,
    PromptPlay/src/i18n/he/common.json
  </files>
  <action>
    **1. Create `PromptPlay/src/features/onboarding/AccountPromptModal.tsx`:**
    - Props: `{ visible: boolean, onSignUp: () => void, onSkip: () => void }`
    - Full-screen modal overlay with semi-transparent background
    - Content: encouraging message ("Save your progress!"), brief explanation (sync across devices), two buttons:
      - "Create Account" (primary, calls onSignUp)
      - "Maybe Later" (secondary/text, calls onSkip)
    - RTL: `paddingStart/End`, `textAlign` from `isRTL()`
    - No guilt language. Frame as benefit ("Keep your streaks safe") not loss ("You'll lose your progress").
    - Use `Modal` from react-native with `transparent` and `animationType="fade"`

    **2. Modify `PromptPlay/src/features/lesson/LessonScreen.tsx`:**
    - Add state: `const [showAccountPrompt, setShowAccountPrompt] = useState(false)`
    - Add state: `const [accountPromptShown, setAccountPromptShown] = useState(false)` (prevent showing twice in same session)
    - In `handleFinish`, AFTER `completeLesson` and all store updates, BEFORE navigating:
      - Read `completedLessons.length` from store (after the completeLesson call, so it includes the just-completed lesson)
      - Check condition: `completedLessons.length >= 2 && !accountPromptShown`
      - NOTE: We check `>= 2` because REQUIREMENTS.md ONBR-04 says "after lesson 2-3" and we want to show it at the earliest point in that range
      - For now, do NOT check auth session (that comes in plan 04-03). Just check completion count.
      - If condition met: `setShowAccountPrompt(true)` and return (don't navigate yet)
      - If condition not met: proceed with existing navigation (level-up check then router.replace)
    - Add `AccountPromptModal` to the render:
      - `onSignUp`: For now, just dismiss the modal and navigate home. Plan 04-03 will wire real auth.
      - `onSkip`: `setShowAccountPrompt(false)`, `setAccountPromptShown(true)`, proceed with normal navigation flow (level-up check then router.replace)
    - The modal should appear AFTER the completion celebration animation finishes, AFTER the user taps Continue, triggered by handleFinish logic

    **3. Add i18n keys** to both `en/common.json` and `he/common.json`:
    ```
    "accountPrompt.title": "Save your progress!" / "!שמרו את ההתקדמות שלכם"
    "accountPrompt.description": "Create a free account to sync your progress across devices and keep your streaks safe." / "צרו חשבון חינמי כדי לסנכרן את ההתקדמות שלכם בין מכשירים ולשמור על הרצף."
    "accountPrompt.signUp": "Create Account" / "צרו חשבון"
    "accountPrompt.skip": "Maybe Later" / "אולי אחר כך"
    ```
  </action>
  <verify>
    <automated>cd PromptPlay && npx expo export --platform web 2>&1 | tail -5</automated>
  </verify>
  <done>
    - AccountPromptModal renders with encouraging tone, sign-up and skip buttons
    - Modal appears after completing lesson 2 (completedLessons.length >= 2)
    - Modal does NOT appear before lesson 2 completion
    - Skip dismisses modal and navigates home normally
    - Sign-up button dismisses modal (real auth wiring deferred to 04-03)
    - Modal only shown once per lesson session (accountPromptShown guard)
    - No guilt language in modal copy
  </done>
</task>

</tasks>

<verification>
1. `cd PromptPlay && npx expo export --platform web 2>&1 | tail -5` -- no build errors
2. Flow test: Clear store (or fresh install) -> app shows welcome screen -> select goal -> immediately in lesson 1 -> less than 60 seconds path
3. Flow test: Complete lesson 1 -> no account prompt -> complete lesson 2 -> account prompt appears -> skip -> navigates home
4. Flow test: Relaunch app with dailyGoal set -> tabs appear immediately, no onboarding flash
</verification>

<success_criteria>
- New user reaches first exercise within 60 seconds (welcome -> goal -> lesson 1)
- No signup wall at any point
- Account prompt appears after lesson 2 completion, is skippable
- Returning users see tabs immediately with no flash
- All onboarding text is bilingual (en + he) with RTL-correct layout
</success_criteria>

<output>
After completion, create `.planning/phases/04-skill-tree-onboarding/04-02-SUMMARY.md`
</output>
