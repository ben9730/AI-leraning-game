---
phase: 04-skill-tree-onboarding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - PromptPlay/src/features/skill-tree/useSkillTreeData.ts
  - PromptPlay/src/features/skill-tree/SkillTreeNode.tsx
  - PromptPlay/src/features/skill-tree/ChapterHeader.tsx
  - PromptPlay/app/(tabs)/skill-tree.tsx
autonomous: true
requirements:
  - TREE-01
  - TREE-02
  - TREE-03
  - TREE-04
  - TREE-05

must_haves:
  truths:
    - "All lesson nodes render with correct locked/unlocked/complete visual states based on store data"
    - "Tapping an unlocked node navigates to that lesson; tapping a completed node navigates to review"
    - "Tapping a locked node does nothing"
    - "Chapter groupings are visually distinct with header labels"
    - "The first unlocked-but-not-completed node is visually highlighted and auto-scrolled to on mount"
  artifacts:
    - path: "PromptPlay/src/features/skill-tree/useSkillTreeData.ts"
      provides: "Derived node state hook"
      exports: ["useSkillTreeData", "NodeState", "SkillNode", "ChapterGroup"]
    - path: "PromptPlay/src/features/skill-tree/SkillTreeNode.tsx"
      provides: "Individual skill tree node component"
      exports: ["SkillTreeNode"]
    - path: "PromptPlay/src/features/skill-tree/ChapterHeader.tsx"
      provides: "Chapter section header component"
      exports: ["ChapterHeader"]
    - path: "PromptPlay/app/(tabs)/skill-tree.tsx"
      provides: "Full skill tree screen replacing placeholder"
      min_lines: 50
  key_links:
    - from: "PromptPlay/src/features/skill-tree/useSkillTreeData.ts"
      to: "PromptPlay/src/store/useProgressStore.ts"
      via: "useProgressStore selectors for completedLessons and unlockedLessons"
      pattern: "useProgressStore\\(s => s\\.(completedLessons|unlockedLessons)\\)"
    - from: "PromptPlay/src/features/skill-tree/useSkillTreeData.ts"
      to: "PromptPlay/src/content/curriculum.ts"
      via: "imports chapters array for grouping"
      pattern: "import.*chapters.*from.*curriculum"
    - from: "PromptPlay/app/(tabs)/skill-tree.tsx"
      to: "PromptPlay/app/(lesson)/[lessonId].tsx"
      via: "router.push on node tap"
      pattern: "router\\.push.*lesson"
---

<objective>
Build the visual skill tree screen showing all lesson nodes with lock/unlock/complete states, chapter groupings, progress indicator, and navigation to lessons.

Purpose: Give users a visible map of their learning journey. This is the primary navigation surface for accessing lessons.
Output: Working skill tree tab that renders nodes from curriculum data with correct visual states derived from the progress store.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-skill-tree-onboarding/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From PromptPlay/src/content/schema.ts:
```typescript
export interface Chapter {
  id: number
  title: LocalizedString
  description: LocalizedString
  lessonIds: string[]
}
export interface LocalizedString { en: string; he: string }
```

From PromptPlay/src/content/curriculum.ts:
```typescript
export const chapters: Chapter[] = [
  {
    id: 1,
    title: { en: 'What Can AI Do?', he: '...' },
    description: { en: '...', he: '...' },
    lessonIds: ['lesson-01-what-is-prompting', 'lesson-02-clarity', 'lesson-03-specificity', 'lesson-04-context', 'lesson-05-intent'],
  },
]
export const curriculum: string[] = chapters.flatMap((ch) => ch.lessonIds)
```

From PromptPlay/src/store/types.ts:
```typescript
export interface UserProgress {
  completedLessons: string[]
  unlockedLessons: string[]
  // ... other fields
  completeLesson: (lessonId: string) => void
  unlockLesson: (lessonId: string) => void
}
```

From PromptPlay/src/store/useProgressStore.ts:
```typescript
// Default state includes:
// unlockedLessons: ['lesson-01-what-is-prompting']
export const useHasHydrated = () => useProgressStore(s => s._hasHydrated)
```

From PromptPlay/app/(lesson)/[lessonId].tsx:
```typescript
// Navigation route: router.push('/(lesson)/[lessonId]')
// Receives lessonId from useLocalSearchParams
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Derived state hook + node/header components</name>
  <files>
    PromptPlay/src/features/skill-tree/useSkillTreeData.ts,
    PromptPlay/src/features/skill-tree/useSkillTreeData.test.ts,
    PromptPlay/src/features/skill-tree/SkillTreeNode.tsx,
    PromptPlay/src/features/skill-tree/ChapterHeader.tsx
  </files>
  <behavior>
    - useSkillTreeData returns ChapterGroup[] with correct structure from chapters array
    - A lesson in completedLessons has state 'complete'
    - A lesson in unlockedLessons (but not completed) has state 'unlocked'
    - A lesson in neither array has state 'locked'
    - lesson-01-what-is-prompting is always at least 'unlocked' (default store state)
    - getCurrentLessonId returns the first unlocked-but-not-completed lesson ID, or null if all complete
  </behavior>
  <action>
    Create `PromptPlay/src/features/skill-tree/` directory.

    **useSkillTreeData.ts:**
    - Export types: `NodeState = 'locked' | 'unlocked' | 'complete'`, `SkillNode { lessonId, title: LocalizedString, state: NodeState }`, `ChapterGroup { chapter: Chapter, nodes: SkillNode[] }`
    - Export hook `useSkillTreeData(): ChapterGroup[]` that reads `completedLessons` and `unlockedLessons` from useProgressStore, iterates `chapters` from curriculum.ts, and derives state for each lesson.
    - Export pure function `getCurrentLessonId(chapters: Chapter[], completedLessons: string[], unlockedLessons: string[]): string | null` that finds the first unlocked-but-not-completed lesson. This is extracted as a pure function for testability.
    - To get lesson titles, use `loadLesson(lessonId).content.title` from `@/src/content/loader`. This keeps titles in sync with content.

    **useSkillTreeData.test.ts:**
    - Test getCurrentLessonId with: all locked, one unlocked, some completed + one unlocked, all completed.
    - Test the derivation logic (use pure function variant for unit testing).
    - Use Jest node preset (content tests pattern from Phase 2 decisions).

    **SkillTreeNode.tsx:**
    - Props: `{ lessonId, title: string, state: NodeState, isCurrent: boolean, onPress: () => void, onLayout?: (event: LayoutChangeEvent) => void }`
    - Visual states: `locked` = gray bg + lock icon + disabled, `unlocked` = green border + play icon, `complete` = green bg + checkmark
    - `isCurrent` adds a pulsing border or distinct highlight (use Reanimated `withRepeat(withSpring(...))` for subtle pulse on the current node)
    - RTL: use `paddingStart/End`, `flexDirection: 'row'` (auto-reverses). No `left`/`right`.
    - `accessibilityRole="button"`, `accessibilityState={{ disabled: state === 'locked' }}`

    **ChapterHeader.tsx:**
    - Props: `{ title: string, lessonCount: number, completedCount: number }`
    - Renders chapter title + progress text (e.g., "3/5 complete")
    - RTL: use `textAlign` derived from `isRTL()`, `paddingStart/End`
  </action>
  <verify>
    <automated>cd PromptPlay && npx jest src/features/skill-tree/useSkillTreeData.test.ts -x</automated>
  </verify>
  <done>
    - useSkillTreeData derives correct NodeState for each lesson from store arrays
    - getCurrentLessonId finds the user's current position
    - SkillTreeNode renders three visual states with RTL-correct layout
    - ChapterHeader shows title and progress count
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Skill tree screen with scroll-to-current and navigation</name>
  <files>
    PromptPlay/app/(tabs)/skill-tree.tsx
  </files>
  <action>
    Replace the placeholder skill-tree.tsx with the full implementation.

    **Screen structure:**
    - `ScrollView` with `ref` for programmatic scrolling
    - Header area: screen title from `t('skillTree.title')`, overall progress bar showing `completedCount / totalCount` lessons
    - For each ChapterGroup from `useSkillTreeData()`: render `ChapterHeader`, then map `nodes` to `SkillTreeNode` components
    - Each node's `onLayout` records its y-position in a `useRef<Record<string, number>>({})` map

    **Navigation on press:**
    - `state === 'unlocked'`: `router.push('/(lesson)/[lessonId]')` with the node's lessonId. Use template: `router.push({ pathname: '/(lesson)/[lessonId]', params: { lessonId: node.lessonId } })`
    - `state === 'complete'`: Same navigation (allows review). The lesson engine handles replay naturally.
    - `state === 'locked'`: Node is disabled, no press handler fires.

    **Scroll to current (TREE-05):**
    - After first render, use `useEffect` to find the current lesson node (from `getCurrentLessonId`) in the y-offset map
    - Call `scrollRef.current?.scrollTo({ y: offset - 100, animated: true })` to position current node in view
    - Add 300ms delay via `setTimeout` to ensure layout measurements are complete before scrolling

    **Progress indicator (TREE-05):**
    - At the top of the scroll, show a progress bar: `completedLessons.length / curriculum.length * 100`%
    - Use a simple `View` with percentage width and `backgroundColor: '#6C63FF'` inside a gray track

    **Styling:**
    - Background: `#fff`
    - RTL: All padding uses `Start/End`. `textAlign` from `isRTL()`.
    - Node spacing: `marginVertical: 6` per node, `marginBottom: 24` after each chapter section
    - Safe area: Use `paddingTop: 48` for status bar clearance (matching profile.tsx pattern)

    **i18n keys to add** to both `PromptPlay/src/i18n/en/common.json` and `PromptPlay/src/i18n/he/common.json`:
    - `skillTree.progress`: "{{completed}} of {{total}} lessons" / Hebrew equivalent
    - `skillTree.chapterProgress`: "{{completed}}/{{total}}" / same in Hebrew
    - Existing `skillTree.title` key is already present.
  </action>
  <verify>
    <automated>cd PromptPlay && npx expo export --platform web 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Skill tree screen renders all lesson nodes from curriculum.ts chapters
    - Nodes show correct visual state (locked/unlocked/complete) based on store
    - Tapping unlocked/completed nodes navigates to lesson route
    - Locked nodes are disabled
    - Chapter headers with progress counts separate lesson groups
    - Current lesson node is highlighted and scrolled into view on mount
    - Progress bar at top shows overall completion percentage
    - RTL layout works correctly (paddingStart/End, no left/right)
  </done>
</task>

</tasks>

<verification>
1. `cd PromptPlay && npx jest src/features/skill-tree/ -x` -- all unit tests pass
2. `cd PromptPlay && npx expo export --platform web 2>&1 | tail -5` -- no build errors
3. Visual: Skill tree tab shows 5 lesson nodes under Chapter 1 header, lesson 1 is unlocked (green border), lessons 2-5 are locked (gray)
</verification>

<success_criteria>
- Skill tree tab renders all lesson nodes with correct locked/unlocked/complete states
- Tapping an unlocked node navigates to that lesson
- Chapter groupings are visually distinct with headers and progress counts
- Current progress position is highlighted and auto-scrolled to on mount
- Overall progress bar shows completion percentage
- All RTL layout uses directional properties (no left/right)
</success_criteria>

<output>
After completion, create `.planning/phases/04-skill-tree-onboarding/04-01-SUMMARY.md`
</output>
